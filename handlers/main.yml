---
# handlers file for perfsonar-testpoint

- name: SvcRestart TimeSync
  service:
    name: "{{ timesyncsvc }}"
    state: restarted

- name: SvcRestart SSH
  service:
    name: "{{ ssh_service }}"
    state: restarted

